# Skill Submodules

Add each standalone skill repository here as a Git submodule:

```bash
git submodule add <skill-repo-url> skills/<skill-name>
```

Do not copy nested `.git` directories into this folder manually. Use submodules
so GitHub can display each skill as a linked independent repository.

